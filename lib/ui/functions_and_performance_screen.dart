import 'package:flutter/material.dart';

class FunctionsAndPerformanceScreen extends StatelessWidget {
  const FunctionsAndPerformanceScreen({Key key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('機能・性能'),
      ),
      body: const Center(
        child: Text('機能・性能'),
      ),
    );
  }
}
